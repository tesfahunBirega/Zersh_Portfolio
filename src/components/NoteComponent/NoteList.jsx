// components/NoteList.js
import React from "react";
import { List, Button, Table } from "antd";

const NoteList = ({ notes, deleteNote }) => {
  // const noteColumn =
  return <Table dataSource={notes} />;
};

export default NoteList;
